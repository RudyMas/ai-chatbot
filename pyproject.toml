[project]
name = "ai-chatbot"
version = "0.0.1"
description = "Config-driven local AI chatbot framework (modular, Ollama-first)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["PyYAML>=6.0.1"]
