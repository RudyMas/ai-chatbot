# LocalAI Bot (modular, config-driven)

Step 1: minimal skeleton that reads config and prints bot identity.  
No LLM call yet. Next step will add Ollama.

## Run
```bash
python -m pip install -e .
python src/main.py
